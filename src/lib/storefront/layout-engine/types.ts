// LayoutEngine uses PublishedSnapshot as input and StorefrontDocument as output.
// All types are imported from the canonical contract definitions.
// No additional types are defined here.

export type {
  PublishedSnapshot,
  WebsiteAggregate,
  HeroContent,
  LayoutSnapshot,
  ThemeSnapshot,
  NavigationItem,
  RenderingHints,
} from "@/types/snapshot";

export type { StorefrontDocument } from "@/types/storefront";
